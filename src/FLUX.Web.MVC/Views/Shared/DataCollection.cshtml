@model IList<string>

@for (int i = 0; i < Model.Count; i++)
{
    if (string.IsNullOrEmpty(Model[i]))
    {
       <img src="" class="empty" />
    }
    else
    {
        <span class="layer@(i)">@Model[i]</span>
    }
    <br />
}