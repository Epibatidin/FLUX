@model bool[]

@for (int i = 0; i < Model.Length; i++)
{
    string classes = "colorMarker";
    if (Model[i])
    {
        classes += " color" + i;
    }
    <div class="@classes" >&nbsp;</div>
}